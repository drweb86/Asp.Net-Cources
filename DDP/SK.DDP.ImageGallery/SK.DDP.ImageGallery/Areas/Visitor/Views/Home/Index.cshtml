@model IEnumerable<SK.DDP.BL.AlbumViewModel>

@{
    ViewBag.Title = "Albums";
}

<h2>Albums</h2>

<p>
    @Html.ActionLink("Add Album", "AddAlbum")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.ActionLink(item.Name, "Details", new { /* id=item.PrimaryKey */ })
        </td>
        <td>
            @Html.ActionLink("Rename", "RenameAlbum", new { id=item.Album_UID }) |
            
            @Html.ActionLink("Delete", "DeleteAlbum", new { id=item.Album_UID })
            @*TODO: add confirmation*@
        </td>
    </tr>
}

</table>
